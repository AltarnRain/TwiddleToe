<Window x:Class="TwiddleToe.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TwiddleToe.UI.Views"
        mc:Ignorable="d"
        Title="TwiddleToe" Height="100" Width="800" ResizeMode="NoResize" Topmost="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Margin="10">Doe een Quiz!</Button>
        <Button Command="{Binding OpenUsers}" Grid.Column="1" Margin="10">Gebruikers</Button>
        <Button Command="{Binding OpenSubjects}" Grid.Column="2" Margin="10">Onderwerpen</Button>
        <Button Grid.Column="3" Margin="10">Vragen lijsten</Button>
        <Button Grid.Column="4" Margin="10">Geschiedenis</Button>
    </Grid>
</Window>
